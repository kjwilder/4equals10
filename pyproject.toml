[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "fourten"
description = "4 equals 10"
readme = "README.md"
requires-python = ">=3.7"
version = "0.1"

[project.scripts]
fourten = "fourten.solver:main"
